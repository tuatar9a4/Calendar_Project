<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingHorizontal="20dp"
    android:paddingBottom="20dp"
    android:paddingTop="12dp"
    android:background="@drawable/bottom_sheet_top_border_bg"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/tvBottomSheetTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@id/rcSheetList"
        style="@style/Font24Weight600"
        android:textColor="@color/black"
        android:text="title"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcSheetList"
        android:layout_width="match_parent"
        app:layout_constrainedHeight="true"
        android:layout_height="280dp"
        app:layout_constraintTop_toBottomOf="@id/tvBottomSheetTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="10dp"
        tools:itemCount="12"
        tools:listitem="@layout/custom_bottom_sheet_dialog_item"
        />
</androidx.constraintlayout.widget.ConstraintLayout>