<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devd.calenderbydw">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CalendarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icons_calendar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icons_calendar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalenderByDW"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.devd.calenderbydw.SplashActivity"
            android:exported="true"
            android:theme="@style/mainStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.devd.calenderbydw.CalendarWidget"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.devd.calenderbydw.COLLECTION_VIEW_ACTION"/>
                <action android:name="com.devd.calenderbydw.BT_REFRESH_ACTION"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_info" />
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="com.devd.calenderbydw.MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/mainStyle">

        </activity>

        <service
            android:name="com.devd.calenderbydw.adapters.WidgetAdapter"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service
            android:name="com.devd.calenderbydw.serviece.DataRecevieService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:path="/taskdata"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />

                <data
                    android:host="*"
                    android:path="*"
                    android:scheme="wear" />
            </intent-filter>

        </service>
    </application>

</manifest>